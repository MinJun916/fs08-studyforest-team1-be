@host = http://localhost:3000
@studyId = b8f11e76-0a9e-4b3f-bccf-8d9b4fbf331e
@habitId = 11111111-1111-1111-1111-111111111111
@password = 1234
@newName = 새로운 습관 이름

// 모든 습관 목록 확인 (검증용)
GET {{host}}/habits
Accept: application/json

###

// 오늘부터 새로운 습관 생성
// POST /habit-lifecycle/create/:studyId
POST {{host}}/habit-lifecycle/create/{{studyId}}
Content-Type: application/json

{
  "name": "{{newName}}"
}

###

// 생성/수정/종료 후 오늘의 습관 조회 (비밀번호 필요)
// GET /habits/:studyId/today?password=...
GET {{host}}/habits/{{studyId}}/today?password={{password}}
Accept: application/json

###

// 습관 이름 변경
// PATCH /habit-lifecycle/:habitId
PATCH {{host}}/habit-lifecycle/{{habitId}}
Content-Type: application/json

{
  "name": "{{newName}}",
  "studyId": "{{studyId}}"
}

###

// 오늘부터 종료 (과거 기록에는 영향 없음)
// DELETE /habit-lifecycle/:habitId
DELETE {{host}}/habit-lifecycle/{{habitId}}
Content-Type: application/json

{
  "studyId": "{{studyId}}"
}

###

// 종료 후 오늘의 습관 재확인 (오늘부터 비노출)
GET {{host}}/habits/{{studyId}}/today?password={{password}}
Accept: application/json

