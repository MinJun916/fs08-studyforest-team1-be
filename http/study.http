@host = http://localhost:3000
@studyId = 11111111-1111-1111-1111-111111111111

### Get studies (list) - supports offset, limit, order=newest|oldest
GET {{host}}/study?offset=0&limit=5&order=newest

### Get one study by id
# Note: route in code is defined as '/study:id' so the id is appended without an extra slash
GET {{host}}/study{{studyId}}

### Create a study (POST /study)
POST {{host}}/study
Content-Type: application/json

{
	"nickName": "tester",
	"studyName": "HTTP Test Study",
	"description": "샘플 설명입니다.",
	"backgroundImg": "green",
	"password": "secret-password"
}

### Patch (update) a study
PATCH {{host}}/study{{studyId}}
Content-Type: application/json

{
	"studyName": "Updated Study Name",
	"description": "업데이트된 설명"
}

### Delete a study
DELETE {{host}}/study{{studyId}}

